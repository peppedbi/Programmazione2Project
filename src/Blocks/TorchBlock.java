package Blocks;

import javafx.scene.paint.Color;

public class TorchBlock extends AbstractSolidBlock{
    public TorchBlock() {
        super();
        blockname = "Torch";
        contenuto = 'T';
        color = Color.RED;
    }
}
